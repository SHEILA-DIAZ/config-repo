server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bd_cliente_dev
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

mensaje:
  texto: Configuración DEFAULT de Cliente Service
